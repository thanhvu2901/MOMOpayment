<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paypal Paymen Demo</title>
</head>
<body>
    {{{body}}}

    <style>
        .item-heading {
            display: flex;
            margin-top: 30px;
            margin-left: 30px;
            margin-bottom: 50px;
            position: relative;
            font-weight: 600;
            font-size: 20px;
        }
        
        .item-name-title {
            position: absolute;
            left : 0;

        }
        .item-quantity-title {
            position: absolute;
            left: 300px;
        }

        .item-price-title {
            position: absolute;
            left: 400px;

        }
        .item {
            display: flex;
            margin-top: 40px;
            margin-left: 30px;
            position: relative;
        }

        .item-name {
            position: absolute;
            left: 0px;
        }

        .item-quantity {
            position: absolute;
            left: 320px;
        }

        .item-price {
            position: absolute;
            left: 400px;
        }

        .button-submit {
            margin-top: 50px;
            margin-left: 30px;
            height: 30px;
            width: 200px;
            background-color: #ae2070;
            border: 2px solid #000;
            cursor: pointer;

        }

        html,
body {
    color: #ae2070;
    font-family: 'Roboto', sans-serif;
}

.checkout-button {
    width: 40%;
    display: flex;
    align-items: center;
    border: 1px solid #ae2070;
    padding: 5px;
    border-radius: 5px
}

.checkout-button img {
    padding-left: 10px;
    height: 50px;
    width: 50px;
}

.content {
    padding-left: 15px;
}

.content .checkout-title {
    color: #000;
    font-size: 13pt;
    font-weight: 600;
}

.content .description {
    color: #666
}


    </style>
</body>
</html>